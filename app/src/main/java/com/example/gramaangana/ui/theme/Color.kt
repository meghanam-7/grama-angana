package com.example.gramaangana.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand colors (same in both modes) ──────────────────────
val PrimaryIndigo = Color(0xFF4F46E5)
val PrimaryIndigoLight = Color(0xFF6366F1)
val PrimaryIndigoDark = Color(0xFF3730A3)

// ── Light mode colors ───────────────────────────────────────
val BackgroundGray = Color(0xFFF0F2F8)
val SurfaceWhite = Color(0xFFFFFFFF)
val TextPrimary = Color(0xFF1E1B4B)
val TextSecondary = Color(0xFF6B7280)
val TextOnPrimary = Color(0xFFFFFFFF)

// ── Status colors (same in both modes) ──────────────────────
val GreenFree = Color(0xFF22C55E)
val AmberPending = Color(0xFFF59E0B)
val RedBooked = Color(0xFFEF4444)

// ── Badge colors ─────────────────────────────────────────────
val AiBadgeBg = Color(0xFFFDF4FF)
val AiBadgeText = Color(0xFFD946EF)